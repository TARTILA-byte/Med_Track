import React from "react";

const SearchBar = () => {
  return (
    <input
      type="text"
      className="search-input"
      placeholder="Search medicine..."
    />
  );
};

export default SearchBar;
